# Maintainer: Alex Creio <ctlos@protonmail.com>

pkgname=ctlos-system
pkgdesc='System Settings ctlos'
pkgver=1.4.1
pkgrel=1
arch=('any')
url="https://github.com/ctlos/ctlos-system"
license=('GPL')
makedepends=('git')
depends=(
)
source=("git+$url.git")
sha256sums=('SKIP')
install=README.install

package() {
  cd ${srcdir}/${pkgname}
  # install -d $pkgdir/etc/ctlos-system
  # install -d $pkgdir/usr/local/bin
  # install -d $pkgdir/root

  # cp -rf bin etc root "$pkgdir/etc/ctlos-system"
}
